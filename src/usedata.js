// import { useMemo } from "react";

// export function useBalance(transactions) {
//     return useMemo(() => {
//         return transactions.reduce(
//             (acc, t) =>
//                 acc + (t.type === 'income'
//                     ? Number(t.amount)
//                     : -Number(t.amount)),
//             0
//         );
//     }, [transactions]);
// }


